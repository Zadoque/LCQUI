\newpage
% =================================================
\section{Regras de Segurança do Firestore (Security Rules)}
% =================================================

\begin{explicacao}[Por que as Security Rules são obrigatórias]
Toda leitura via \texttt{onSnapshot} no frontend passa \textbf{diretamente} pelo Firestore SDK, \textbf{sem} passar pelas Cloud Functions. Portanto, as Cloud Functions são a camada de autorização para escrita, mas as \textit{Security Rules} são a camada de autorização para \textbf{leitura direta}. Sem rules corretas, qualquer usuário autenticado poderia ler qualquer coleção.
\end{explicacao}

\begin{regra}[Princípio: Deny-All por Padrão]
A regra raiz do arquivo \texttt{firestore.rules} deve ser:\\
\texttt{allow read, write: if false;}\\
Todas as exceções são declaradas explicitamente abaixo.
\end{regra}

\begin{regra}[M9 --- interpretação obrigatória das Rules]
A matriz de autorização da Seção~7/M9 governa esta seção. Toda exceção de
leitura direta deve exigir: \texttt{request.auth != null}, documento
\texttt{Usuarios/uid} existente e \texttt{ativo=true},
\texttt{request.auth.token.versao\_permissoes} igual à versão persistida,
claim de papel pertencente à lista fechada e a relação persistida de escopo ou
ownership aplicável. Campo de token ausente, versão divergente ou falha de
\texttt{get()} nega. Claim não basta para Gestor\_Almoxarifado: o vínculo N:N
do almoxarifado também é obrigatório.

Nenhuma mutação de domínio, papel, vínculo, notificação, auditoria, operação
M7, lock, configuração de estoque, cache, rate limit ou coleção interna é
permitida diretamente ao cliente. As tabelas históricas abaixo não concedem
write quando contrariem esta regra. Regras não são filtros: consulta ampla que
possa retornar documento proibido é negada ou substituída por endpoint
autorizado. O Admin SDK ignora Rules; cada callable/backend deve aplicar M9 e
as pré-condições de domínio dentro da própria transação.
\end{regra}

\subsection{Regras por Coleção}

\small

\small
\setlength{\tabcolsep}{5pt}
\small
\setlength{\tabcolsep}{4pt}
\begin{longtable}{>{\raggedright\arraybackslash}p{0.32\textwidth} >{\raggedright\arraybackslash}p{0.62\textwidth}}
\toprule
\textbf{Coleção} & \textbf{Regra}\\
\midrule
\endfirsthead
\toprule
\textbf{Coleção} & \textbf{Regra} (continuação)\\
\midrule
\endhead
\bottomrule
\endfoot
\nolinkurl{Usuarios/\{uid\}} & Read: \texttt{request.auth.uid == uid} (usuário lê só o próprio documento). Write: negado ao cliente (só Cloud Function).\\
\nolinkurl{Usuarios/\{uid\}/Turmas/\{turmaId\}} & Read: \texttt{request.auth.uid == uid} (o aluno lê em tempo real apenas as turmas em que está matriculado). Write: negado ao cliente (gerenciada exclusivamente pelas Cloud Functions).\\
\texttt{Professor/\{uid\}}, \texttt{Aluno/\{uid\}} & Read: próprio usuário OU Chefe Geral. Write: negado. Outros usuários recebem apenas projeção sanitizada de nome, centro e laboratório por endpoint autorizado; nunca CPF, matrícula ou e-mail pessoal.\\
\nolinkurl{Bem\_Patrimonial/\{id\}/Historico\_Patrimonio/\{eventoId\}} & Read: Chefe Geral ou Gestor de Bens Patrimoniais; Write: negado. Regra de collection-group específica para Historico\_Patrimonio, sem abranger históricos acadêmicos.\\
\texttt{Chefe\_Geral}, \texttt{Gestor\_Almoxarifado}, e demais coleções administrativas de papel & Read: \texttt{request.auth.uid == docId} (cada usuário lê só o próprio documento de papel). Write: negado ao cliente.\\
\nolinkurl{Turma/\{turmaId\}} & Read: usuário é professor dono (\texttt{resource.data.id\_professor == request.auth.uid}) OU tem vínculo canônico atual em \texttt{Turma/\{turmaId\}/Alunos/\{uid\}} OU \texttt{'Chefe\_Geral' in request.auth.token.roles} (escopo Q13). Write: negado ao cliente (Cloud Functions); \texttt{status == 'Arquivada'} não habilita escrita alguma (Q08).\\
\nolinkurl{Turma/\{turmaId\}/Alunos} & Read: \texttt{request.auth.uid == resource.data.id\_aluno} OU membro com vínculo canônico atual, apenas em projeção mínima sem matrícula/e-mail. O vínculo canônico é a autoridade de matrícula; o espelho \nolinkurl{Usuarios/\{uid\}/Turmas} nunca autoriza. Write: negado.\\
\nolinkurl{Convite\_Aluno/\{id\}} & Read/Write: negado ao cliente. Criação, reenvio, aceitação e expiração são exclusivas de Cloud Function; token e hash nunca são lidos pelo cliente.\\
\nolinkurl{Turma/\{turmaId\}/Posts} & Read: professor dono, membro com vínculo canônico atual ou Chefe (Q13); Post com \texttt{removido\_da\_apresentacao = true} é legível apenas pelo professor dono e pelo Chefe, nunca por colegas. Write: negado; turma \texttt{Arquivada} nega toda escrita acadêmica, inclusive edição e moderação (Q08).\\
\nolinkurl{Turma/\{turmaId\}/Posts/\{postId\}/Comentarios} & \texttt{allow read, write: if false;} (Negado ao cliente. Leitura filtrada provida via Cloud Function \texttt{listarComentariosPost}).\\
\nolinkurl{Usuarios/\{uid\}/Notificacoes} & Read: \texttt{request.auth.uid == uid}. Write: negado ao cliente (criação e marcação de leitura são server-owned; ``Limpar tudo'' marca \texttt{lida = true}, nunca \texttt{DELETE}).\\
\nolinkurl{Almoxarifado/\{almoxId\}/Estoques\_Configurados/\{configId\}} & Read: \texttt{Chefe\_Geral} ou \texttt{Gestor\_Almoxarifado} com vínculo ao almoxarifado. Write: negado ao cliente (configuração de escassez server-owned).\\
\nolinkurl{Resumo\_Reagente/\{resumoId\}} & Read: qualquer usuário com papel ativo no sistema (token de acesso válido). Write: negado ao cliente.\\
\nolinkurl{Resumo\_Reagente/\{resumoId\}/Especificacoes/\{specId\}} & Read: qualquer usuário com papel ativo no sistema (catálogo público interno). Write: negado ao cliente.\\
\texttt{Frasco\_Reagente} & Read: papel \texttt{roles} no token contém \texttt{Chefe\_Geral} OU \texttt{Gestor\_Almoxarifado} OU \texttt{Professor} OU \texttt{Bolsista} OU \texttt{Aluno}. Write: negado ao cliente.\\
\texttt{Emprestimo\_Reagente} & Read: \texttt{Chefe\_Geral} OU \texttt{Gestor\_Almoxarifado} OU o próprio usuário que retirou (\texttt{resource.data.id\_usuario\_retirou == request.auth.uid}). Write: negado.\\
\texttt{Bem\_Patrimonial} & Read: Chefe Geral, Gestor de Bens Patrimoniais ou Professor. Write: negado ao cliente.\\
\texttt{Requisicao\_Edicao\_Bem\_Patrimonial} & Read: o solicitante (\texttt{resource.data.id\_usuario\_solicitante == uid}) OU gestores de bens (papel no token). Write: negado.\\
\texttt{Requisicao\_Adicao\_Bem\_Patrimonial} & Idem acima.\\
\texttt{Locks\_Requisicao\_Patrimonio}, \texttt{Chaves\_Unicas}, \texttt{Eventos\_Processados}, \texttt{Controle\_Papeis}, \texttt{Operacoes} & Read/Write: negado ao cliente (exclusivo de Cloud Function).\\
\texttt{Registro\_de\_Auditoria} & Read: \texttt{Chefe\_Geral} no token. Write: negado ao cliente.\\
\texttt{Roteiro\_Experimento} & Read: \texttt{resource.data.id\_professor\_upload == uid} OU \texttt{uid} pertence ao array \texttt{professores\_compartilhados}. Write: negado ao cliente (criação, compartilhamento e revogação são server-owned). Aluno e Chefe Q13 não leem a coleção raiz diretamente: o download é mediado por endpoint autorizado que revalida vínculo canônico atual e Post acessível (aluno) ou escopo de moderação/auditoria (Chefe), confere a geração persistida e emite URL curta. \texttt{file\_url} é legado e não autoriza. A remoção, a perda de vínculo e a revogação impedem novas emissões; uma URL já emitida não é revogada retroativamente e permanece utilizável pelo prazo curto, que não é credencial permanente.\\
\texttt{Contador\_Codigo\_Frasco} & Read: \texttt{request.auth != null} (qualquer logado lê). Write: negado ao cliente (só Cloud Function atualiza o sequenciador).\\
\texttt{Almoxarifado} & Read: qualquer usuário com papel ativo no sistema (token de acesso válido). Write: negado ao cliente.\\
\texttt{Local} & Read: qualquer usuário com papel ativo no sistema. Write: negado ao cliente.\\
\texttt{Materia} & Read: qualquer usuário com papel ativo no sistema. Write: negado ao cliente.\\
\texttt{Substancia\_Quimica} & Read: qualquer usuário com papel ativo no sistema (catálogo público interno). Write: negado ao cliente.\\
\texttt{Lote} & Read: papel \texttt{roles} no token contém \texttt{Chefe\_Geral} OU \texttt{Gestor\_Almoxarifado} (leitura restrita). Write: negado ao cliente.\\
Coleções de Relatórios e Materializações (\texttt{Resumo\_*\_Diario}, \texttt{Atividade\_Gestor\_*\_Mensal}) & Read: \texttt{Chefe\_Geral}, \texttt{Gestor\_Almoxarifado} ou \texttt{Gestor\_Bens\_Patrimoniais} conforme o respectivo domínio. Write: negado ao cliente.\\
\end{longtable}


\begin{regra}[Leitura de Custom Claims nas Security Rules]
O papel do usuário fica disponível nas Security Rules como \texttt{request.auth.token.roles}. Exemplo:\\
\texttt{function temPapel(papel) \{}\\
\texttt{\quad return papel in request.auth.token.roles;}\\
\texttt{\}}\\
Isso garante consistência com o mesmo Custom Claim usado pelas Cloud Functions (\texttt{resolverPapeisDoToken}).
\end{regra}


\subsection{Contrato de acesso para os fluxos detalhados}
\label{sec:acesso-detalhado}

\begin{regra}[Auditoria de transições críticas e estado físico]
As transições de localização para \texttt{EXTRAVIADO} ou \texttt{LOCALIZADO}, entrada ou saída de \texttt{em\_quarentena}, desativação ou exclusão lógica nunca devem ser expostas para escrita direta do cliente. O backend deve validar as condições de contorno, registrar motivo e autor da ação, e garantir efeitos colaterais (como encerramento de empréstimos), persistindo toda a operação no \texttt{Historico\_Frasco\_Reagente} de forma atômica.
\end{regra}

Além do papel, validar o escopo atual. DP-D01 determina consulta de conta ativa em mutações de alto impacto conforme a seção 10; leituras e demais mutações podem confiar no JWT até renovação. Atualizar Custom Claims não invalida imediatamente o token emitido. Versão de permissões e reconciliação não constituem garantia universal já implementada. Preferência de papel no cliente só altera apresentação.

Leituras de membros, posts, comentários e PDFs de turma exigem professor responsável ou vínculo ativo; colegas recebem projeção mínima, sem e-mail/matrícula. Requisições são visíveis ao solicitante, gestor patrimonial e Chefe. Empréstimos completos são visíveis ao retirante, gestor vinculado ao almoxarifado e Chefe; demais leitores do catálogo recebem somente disponibilidade. Histórico operacional segue o mesmo escopo, e auditoria global fica com a chefia. Diretórios usados em seletores devem ter endpoints/projeções mínimas autorizadas, não leitura irrestrita de Usuarios.

Mutações de domínio e locks são exclusivas do servidor, inclusive marcar notificações lidas; não permitir delete de notificações para implementar Limpar tudo. Regras devem cobrir explicitamente Resumo\_Reagente/id/Especificacoes, Turma e suas subcoleções e Bem\_Patrimonial/id/Historico\_Patrimonio. Consultas collection-group exigem regras/índices próprios. Contador, unicidade e controles de papéis são técnicos; somente resposta mínima autorizada é exposta ao operador.

Storage valida dono, caminho, tipo e tamanho: foto inferior a 5 MiB, baixa inferior a 10 MiB e roteiro inferior a 15 MiB. Backend verifica arquivo existente e assinatura de conteúdo antes de referenciar; metadados não bastam para provar formato. Leitura de roteiro exige propriedade, compartilhamento ou post de turma acessível. Firestore e Storage não têm transação única: a geração é persistida no documento e comparada na transação do Firestore, enquanto a inspeção do objeto no Storage é etapa externa; a janela residual é coberta pela imutabilidade do objeto vinculado e pela reconciliação de órfãos, sem atomicidade entre serviços. Comprovante de baixa segue escopo patrimonial e não pode ser removido após vinculação. Não conceder leitura de todo Storage apenas porque o usuário está autenticado.

Estas são exigências da especificação; firestore.rules e storage.rules atuais ainda divergem, conforme AUDITORIA\_ATUALIZADA.md. A presença de deny-all genérico não anula permissões amplas de matches específicos.

\subsection{Moderação: autorização por documento e resposta filtrada}
Para M12.1, criar/editar Post é server-owned do professor dono; criar/editar
Comentário é do autor com vínculo canônico atual; moderar Post/Comentário é do
professor dono ou do Chefe por Q13, sempre com motivo, operador e instante
atômicos e histórico imutável. Turma \texttt{Arquivada} nega toda escrita
acadêmica, sem exceção administrativa (Q08). A leitura do \texttt{texto} de
Comentário moderado é mascarada por endpoint autorizado; o autor recebe o
original marcado e o professor dono/Chefe recebem original com histórico. Rules
não são filtros: negar a leitura direta de \texttt{Comentarios} e do histórico
e não deixar \texttt{removido\_da\_apresentacao} vazar a colegas.

Firestore Rules autorizam ou negam documentos completos; não fazem projeção nem substituição do campo texto. Não implementar ``texto vazio via Rules''. Para o modelo físico descrito, negar leitura direta de Comentarios e histórico a quem não pode conhecer o original; a listagem de comentários é fornecida por endpoint autenticado que valida vínculo com a turma e devolve somente aviso institucional para comentários moderados de colegas. Autor recebe original marcado; Professor responsável e Chefe Geral recebem conteúdo necessário à auditoria. Não retornar original em campos auxiliares, cache, notificações ou mensagens de erro. Consulta ampla cliente que possa incluir documentos proibidos deve ser rejeitada e substituída pelo endpoint, nunca filtrada apenas no frontend. Backend valida também autoria da edição, motivo e papel de moderação, status ativo da turma e registra histórico/auditoria.

Coleções Controle\_Papeis, Operacoes e Chaves\_Unicas são exclusivas de Admin SDK; leitura e escrita diretas de clientes são negadas. O deny-all atual já as protege, sem allow concorrente; matches explícitos constituem hardening documental. Esta seção descreve o alvo; a implementação atual de moderação permanece pendente.
\paragraph{Turmas, matrícula e convites (M11).}
A leitura direta de \texttt{Turma}, \texttt{Turma/\{id\}/Alunos} e
\texttt{Usuarios/\{uid\}/Turmas} não substitui a autorização: a matrícula
canônica é \texttt{Turma/\{id\}/Alunos/\{uid\}} e o espelho em
\texttt{Usuarios/\{uid\}/Turmas/\{id\}} é apenas projeção de consulta. Colegas
veem exclusivamente projeção mínima de nome e foto, nunca matrícula ou e-mail.
\texttt{Convite\_Aluno} é integralmente server-owned: o cliente não lê token,
hash ou e-mail de terceiros e não escreve convite, aceitação, reenvio ou
expiração. Turma com \texttt{status == 'Arquivada'} é somente leitura (Q08) e o
backend rejeita novo ingresso, novo convite, novo post e novo comentário,
independentemente do token. Fração excepcional do Chefe (Q13) é escopo de
moderação auditada, não ownership, e não transforma o corpo discente em
autoridade de escrita.

\paragraph{Validação binária antes da vinculação (AUDITORIA-7, C011).}
\texttt{registrarBaixaBemPatrimonial}, \texttt{criarPost} (ou \texttt{criarPostTurma}) e \texttt{cadastrarRoteiro} (ou \texttt{cadastrarRoteiroExperimento}) validam o objeto no bucket autorizado, dono, caminho, tamanho e assinatura binária antes de persistir qualquer URL. PDF exige bytes ASCII \texttt{\%PDF-}; JPEG exige hexadecimal \texttt{FF D8 FF}; PNG exige \texttt{89 50 4E 47 0D 0A 1A 0A}. Extensão e MIME declarados não substituem essa verificação. Formato incompatível rejeita toda a confirmação, sem vínculo documental parcial. A versão validada do objeto deve ser preservada contra substituição após a checagem; o backend fixa a geração validada e impede sobrescrita do objeto vinculado. Posts que referenciam roteiro existente verificam também ACL e versão validada do PDF.

\paragraph{Cache, limite e projeções internas.}
As regras a seguir integram o mesmo match de documentos da base, sem grants sobrepostos que permitam acesso cliente. O SDK administrativo acessa pelo backend, cuja callable valida App Check, Auth, RBAC/escopo e 5 solicitações/minuto por UID antes de cache/agregações. Campos derivados de saldo, origem de tara e resolução quantitativa são escritos somente pelo backend; payload cliente não é autoridade para esses campos. A projeção operacional \texttt{Pendencias\_Descarte\_Frasco/\{idFrasco\}} é criada por \texttt{resolverQuarentenaFrasco(PENDENTE\_DE\_DESCARTE)} e consumida por \texttt{descartarFrasco}, sempre via backend; clientes não a leem nem a escrevem.
\begin{lstlisting}[language=TypeScript, title={Coleções internas do dashboard, projeção de descarte e estado do catálogo (contrato documental)}]
match /Sistema_Cache_Dashboard/{document=**} {
  allow read, write: if false;
}
match /Sistema_Rate_Limit_Dashboard/{document=**} {
  allow read, write: if false;
}
match /Pendencias_Descarte_Frasco/{idFrasco} {
  allow read, write: if false;
}
match /Sistema_Catalogo_Reagentes/{document=**} {
  // Estado server-owned do catálogo JSON: leitura apenas pelos papéis que
  // consultam reagentes na matriz canônica da Seção 3 (Aluno incluído por
  // "Consultar estoque de reagentes"); escrita sempre negada ao cliente.
  allow read: if request.auth != null &&
    (hasRole("Chefe_Geral") || hasRole("Gestor_Almoxarifado") ||
     hasRole("Professor") || hasRole("Bolsista") || hasRole("Aluno"));
  allow write: if false;
}
\end{lstlisting}

\begin{regra}[Catálogo JSON e Storage]
O estado \texttt{Sistema\_Catalogo\_Reagentes} é server-owned: o cliente não o escreve. A leitura direta do estado (necessária ao \texttt{subscribe} da tela de reagentes) segue a matriz canônica de busca de reagentes (Chefe\_Geral, Gestor\_Almoxarifado, Professor, Bolsista e Aluno, conforme ``Consultar estoque de reagentes'' da Seção~3); alternativamente, o acesso pode ser mediado por função protegida. O objeto JSON no Storage \textbf{não} fica publicamente acessível de forma que ignore esses papéis. Como o download é mediado por Cloud Function, o documento de estado não armazena um \texttt{downloadURL} permanente público: guarda um \texttt{storage\_path} resolvido por acesso temporário autorizado. Aluno não ganha acesso apenas por estar autenticado, mas pela matriz normativa vigente.
\end{regra}
