\subsubsection{Fluxo de Reagentes: Cadastro, Retirada e Devolução}

\paragraph{Contratos humanos reconciliados (M2.1b).}
O lote é opcional nos dois cadastros: \texttt{idLote} ausente persiste como NULL; a especificação permanece obrigatória. O lote informado é consultado e deve corresponder à especificação. A ausência de lote não implica quarentena, indisponibilidade nem cadastro inválido.

Todo frasco registra, de forma histórica e imutável, \texttt{condicao\_inicial\_cadastro}: \texttt{FECHADO} quando o recipiente entrou fisicamente fechado e \texttt{JA\_ABERTO} quando já estava aberto ao passar ao controle do LCQUI. Essa dimensão é distinta de \texttt{saldo\_desconhecido}, que é uma propriedade metrológica atual. O cadastro de frasco já aberto não fabrica tara nem saldo inicial: inicia com \texttt{peso\_frasco\_vazio = NULL} e \texttt{saldo\_desconhecido = true}, recebendo apenas o peso total medido e, quando conhecido ou legível, o \texttt{conteudo\_nominal} original do fabricante (NULL quando desconhecido, nunca $0$). A tara real é exclusivamente o peso do recipiente efetivamente vazio medido na balança; não existe recalibração parcial de tara a partir de estimativa com produto restante.

A justificativa humana obrigatória deve satisfazer \texttt{trim(texto).length >= 20}. A regra vale somente para os contratos humanos enumerados: entrada manual em quarentena; saída/liberação de quarentena; decisão de quarentena no cadastro vencido; decisão de quarentena na primeira abertura; extravio; reencontro; descarte institucional; recalibração de tara real; autoatendimento; justificativa metodológica Q04/Q14 (preservando seu máximo específico preexistente); e correção administrativa de ação do gestor. Não é regra genérica para toda string \texttt{motivo}, \texttt{detalhe} ou \texttt{observacao}, e não impõe mínimo global ao \texttt{detalhe\_status} persistido. Nos exemplos TypeScript, \texttt{length} é a contagem técnica da string JavaScript após \texttt{trim}; não há avaliação semântica do texto. A validação não modifica o payload original, a canonicalização nem o hash de idempotência. Mensagens automáticas não passam por validação de entrada humana. Pesagem de rotina permanece sob esclarecimento de escopo; não recebe nova exigência por inferência. O mínimo geral não cria máximo; limites específicos preexistentes, como Q04, permanecem.

\begin{lstlisting}[language=TypeScript, title={Validação de justificativa humana obrigatória}]
function validarJustificativaHumana(texto: unknown): void {
  if (typeof texto !== "string" || texto.trim().length < 20) {
    throw new HttpsError("invalid-argument",
      "Justificativa humana exige no mínimo 20 caracteres após trim.");
  }
}
\end{lstlisting}

\begin{lstlisting}[language=TypeScript, title={Cadastro de Frasco Fechado (Regra 6.2.13, item 1)}]
interface CadastroFrascoFechado {
  idResumoReagente: string;
  idOperacao: string;
  idEspecificacaoReagente: string;
  idAlmoxarifado: string;
  idLote?: string;
  conteudoNominal: number; // OBRIGATÓRIO (MET-04)
  pesoTotal: number;
  validadeFechado?: string;
  validadeDesconhecida?: boolean;
  decisaoSeJaVencido?: "QUARENTENA" | "PENDENTE_DE_DESCARTE" | "DISPONIVEL";
  detalheStatus?: string;
}

export const cadastrarFrascoFechado = onCall(async (request) => {
  const dados = request.data as CadastroFrascoFechado;
  await validarPermissao(request, ["Chefe_Geral", "Gestor_Almoxarifado"]);

  const crypto = require("crypto");
  const { DateTime } = require("luxon");
  const { idOperacao, ...payloadRelevante } = dados;
  const payloadString = JSON.stringify(payloadRelevante, Object.keys(payloadRelevante).sort());
  const payloadHash = crypto.createHash("sha256").update(payloadString).digest("hex");

  const agora = new Date();
  const validadeFechadoStr = dados.validadeDesconhecida ? null : (dados.validadeFechado ?? null);
  const validadeData = validadeFechadoStr ? DateTime.fromISO(validadeFechadoStr, { zone: "America/Sao_Paulo" }).endOf("day").toJSDate() : null;
  const venceuNoCadastro = Boolean(validadeData && validadeData <= agora);

  if (venceuNoCadastro && !dados.decisaoSeJaVencido) {
    throw new HttpsError("failed-precondition", "O frasco está vencido no cadastro e exige uma decisão do gestor.");
  }
  if (dados.decisaoSeJaVencido === "QUARENTENA") {
    validarJustificativaHumana(dados.detalheStatus);
  }

  // Execução atômica no Firestore: Sequenciamento + Cadastro do Frasco
  return admin.firestore().runTransaction(async (tx) => {
    await validarGestorDoAlmoxarifadoTx(tx, request.auth!.uid, dados.idAlmoxarifado);

    const operacaoRef = admin.firestore().collection("Operacoes").doc(dados.idOperacao);
    const operacaoSnap = await tx.get(operacaoRef);
    if (operacaoSnap.exists) {
      const op = operacaoSnap.data()!;
      if (op.uid === request.auth!.uid && op.tipo_operacao === "CADASTRAR_FECHADO" && op.payload_hash === payloadHash) {
        return op.resultado;
      }
      throw new HttpsError("already-exists", "id_operacao reutilizada com ator ou payload divergente.");
    }

    const resumoRef = admin.firestore().collection("Resumo_Reagente").doc(dados.idResumoReagente);
    const especRef = resumoRef.collection("Especificacoes").doc(dados.idEspecificacaoReagente);
    
    const [resumoSnap, especSnap] = await Promise.all([tx.get(resumoRef), tx.get(especRef)]);
    
    if (!especSnap.exists) throw new HttpsError("not-found", "Especificação de reagente não encontrada.");
    const resumo = resumoSnap.data()!;
    const estadoFisico = resumo.estado_fisico;
    const densidade = especSnap.data()!.densidade;

    if (dados.idLote) {
      const loteSnap = await tx.get(admin.firestore().collection("Lote").doc(dados.idLote));
      if (!loteSnap.exists) throw new HttpsError("not-found", "Lote não encontrado.");
      if (loteSnap.data()!.id_especificacao_reagente !== dados.idEspecificacaoReagente)
        throw new HttpsError("failed-precondition", "Lote não pertence à especificação informada.");
      
      const loteMatSnap = await tx.get(admin.firestore().collection("Lote_Materializado").doc(dados.idLote));
      const countAtual = loteMatSnap.exists ? (loteMatSnap.data()!.qtd_frascos_cadastrados || 0) : 0;
      const qtdComprados = loteSnap.data()!.qtd_frascos_comprados as number;
      if (countAtual >= qtdComprados)
        throw new HttpsError("failed-precondition", `Limite de frascos do lote atingido (${qtdComprados} comprados).`);
    }

    // MET-04: conteudoNominal obrigatório para frasco FECHADO
    if (!dados.conteudoNominal || dados.conteudoNominal <= 0) {
      throw new HttpsError("invalid-argument", "Frasco fechado exige conteúdo nominal positivo.");
    }

    let pesoVazio: number;
    if (estadoFisico === "LIQUIDO") {
      if (!densidade || densidade <= 0) throw new HttpsError("failed-precondition", "Líquido exige densidade positiva.");
      pesoVazio = dados.pesoTotal - (dados.conteudoNominal * densidade);
      if (pesoVazio <= 0) {
        throw new HttpsError("invalid-argument", "Peso total incompatível com o volume nominal para esta densidade.");
      }
    } else {
      pesoVazio = dados.pesoTotal - dados.conteudoNominal;
      if (pesoVazio <= 0) {
        throw new HttpsError("invalid-argument", "Peso total incompatível com a massa nominal informada.");
      }
    }

    // 1. Gera o código sequencial estrito LCQUI-N
    const { novoCodigo } = await obterProximoCodigoFrascoTx(tx);

    // 2. Persiste o frasco com o código gerado
    const frascoRef = admin.firestore().collection("Frasco_Reagente").doc();
    tx.set(frascoRef, {
      id_almoxarifado: dados.idAlmoxarifado,
      id_lote: dados.idLote ?? null,
      id_especificacao_reagente: dados.idEspecificacaoReagente, // projeção física
      id_resumo_reagente: dados.idResumoReagente,
      eh_higroscopico: resumo.eh_higroscopico, // snapshot servidor imutável
      codigo_frasco: novoCodigo,
      conteudo_nominal: dados.conteudoNominal ?? null,
      peso_no_cadastrado: dados.pesoTotal,
      peso_atual: dados.pesoTotal,
      // Tara inicial apenas derivada (peso total - conteúdo nominal) enquanto há produto;
      // não é medição física definitiva. Vira tara real quando o recipiente for pesado vazio.
      peso_frasco_vazio: pesoVazio,
      medida_usada: 0,
      condicao_inicial_cadastro: "FECHADO", // dimensão histórica imutável
      estado_fisico_frasco: "FECHADO",
      disponibilidade: "DISPONIVEL",
      validade_fechado: validadeFechadoStr,
      validade_efetiva: validadeFechadoStr,
      validade_desconhecida: dados.validadeDesconhecida ?? false,
      vencido: venceuNoCadastro,
      em_quarentena: venceuNoCadastro && dados.decisaoSeJaVencido === "QUARENTENA",
      uso_vencido_autorizado: venceuNoCadastro && dados.decisaoSeJaVencido === "DISPONIVEL",
      detalhe_status: venceuNoCadastro
        ? dados.detalheStatus ?? (dados.decisaoSeJaVencido === "PENDENTE_DE_DESCARTE" ? "Frasco vencido aguardando processo institucional de descarte." : null)
        : null,
      cadastrado_em: admin.firestore.FieldValue.serverTimestamp(),
      cadastrado_por: request.auth!.uid,
    });

    if (dados.idLote) {
      tx.set(admin.firestore().collection("Lote_Materializado").doc(dados.idLote), {
        qtd\_frascos\_cadastrados: admin.firestore.FieldValue.increment(1)
      }, { merge: true });
    }

    // M-15: evento CADASTRO no histórico do frasco, gravado na mesma transação
    const historicoRef = admin.firestore().collection("Historico_Frasco_Reagente").doc();
    tx.set(historicoRef, {
      id_frasco_reagente: frascoRef.id,
      id_almoxarifado: dados.idAlmoxarifado,
      id_gestor: request.auth!.uid,
      tipo: "CADASTRO",
      timestamp: admin.firestore.FieldValue.serverTimestamp(),
    });

    const resultado = { idFrasco: frascoRef.id, codigoFrasco: novoCodigo, pesoVazioCalculado: pesoVazio, venceuNoCadastro };
    
    tx.set(operacaoRef, {
      tipo_operacao: "CADASTRAR_FECHADO",
      uid: request.auth!.uid,
      payload_hash: payloadHash,
      resultado,
      status: "CONCLUIDA",
      criado_em: admin.firestore.FieldValue.serverTimestamp(),
      atualizado_em: admin.firestore.FieldValue.serverTimestamp()
    });

    return resultado;
  });
});
\end{lstlisting}

\begin{lstlisting}[language=TypeScript, title={cadastrarFrascoAberto sem fabricar tara nem saldo inicial (M2.1b)}]
// M2.1b: cadastro de frasco já aberto registra apenas dados reais disponíveis.
// Não existem modalidades de tara informada nem de estimativa de volume ou massa.
interface CadastroFrascoAberto {
  idOperacao: string;
  idResumoReagente: string;
  idEspecificacaoReagente: string;
  idAlmoxarifado: string;
  idLote?: string;
  pesoTotalBalanca: number; // peso total atual medido na balança
  conteudoNominal?: number; // rótulo original do fabricante; NULL se desconhecido/ilegível (nunca 0)
  dataAbertura?: string;
  aberturaHistoricaDesconhecida: boolean;
  validadeAberto?: string;
  validadeDesconhecida?: boolean;
  decisaoSeJaVencido?: "QUARENTENA" | "PENDENTE_DE_DESCARTE" | "DISPONIVEL";
  detalheStatus?: string;
}

export const cadastrarFrascoAberto = onCall(async (request) => {
  const dados = request.data as CadastroFrascoAberto;
  await validarPermissao(request, ["Chefe_Geral", "Gestor_Almoxarifado"]);

  const crypto = require("crypto");
  const { DateTime } = require("luxon");
  const { idOperacao, ...payloadRelevante } = dados;
  const payloadString = JSON.stringify(payloadRelevante, Object.keys(payloadRelevante).sort());
  const payloadHash = crypto.createHash("sha256").update(payloadString).digest("hex");

  const agora = new Date();
  const validadeAbertoStr = dados.validadeDesconhecida ? null : (dados.validadeAberto ?? null);
  const validadeData = validadeAbertoStr ? DateTime.fromISO(validadeAbertoStr, { zone: "America/Sao_Paulo" }).endOf("day").toJSDate() : null;
  const jaVencido = Boolean(validadeData && validadeData <= agora);

  if (jaVencido && !dados.decisaoSeJaVencido)
    throw new HttpsError("failed-precondition", "O frasco está vencido no cadastro e exige uma decisão do gestor.");
  if (dados.decisaoSeJaVencido === "QUARENTENA")
    validarJustificativaHumana(dados.detalheStatus);

  return admin.firestore().runTransaction(async (tx) => {
    await validarGestorDoAlmoxarifadoTx(tx, request.auth!.uid, dados.idAlmoxarifado);

    const operacaoRef = admin.firestore().collection("Operacoes").doc(dados.idOperacao);
    const operacaoSnap = await tx.get(operacaoRef);
    if (operacaoSnap.exists) {
      const op = operacaoSnap.data()!;
      if (op.uid === request.auth!.uid && op.tipo_operacao === "CADASTRAR_ABERTO" && op.payload_hash === payloadHash) {
        return op.resultado;
      }
      throw new HttpsError("already-exists", "id_operacao reutilizada com ator ou payload divergente.");
    }

    const resumoRef = admin.firestore().collection("Resumo_Reagente").doc(dados.idResumoReagente);
    const especRef = resumoRef.collection("Especificacoes").doc(dados.idEspecificacaoReagente);
    
    const [resumoSnap, especSnap] = await Promise.all([tx.get(resumoRef), tx.get(especRef)]);
    if (!especSnap.exists) throw new HttpsError("not-found", "Especificação não encontrada.");
    
    const resumo = resumoSnap.data()!;

    if (dados.idLote) {
      const loteSnap = await tx.get(admin.firestore().collection("Lote").doc(dados.idLote));
      if (!loteSnap.exists) throw new HttpsError("not-found", "Lote não encontrado.");
      if (loteSnap.data()!.id_especificacao_reagente !== dados.idEspecificacaoReagente)
        throw new HttpsError("failed-precondition", "Lote não pertence à especificação informada.");
      
      const loteMatSnap = await tx.get(admin.firestore().collection("Lote_Materializado").doc(dados.idLote));
      const countAtual = loteMatSnap.exists ? (loteMatSnap.data()!.qtd_frascos_cadastrados || 0) : 0;
      const qtdComprados = loteSnap.data()!.qtd_frascos_comprados as number;
      if (countAtual >= qtdComprados)
        throw new HttpsError("failed-precondition", `Limite de frascos do lote atingido (${qtdComprados} comprados).`);
    }

    // M2.1b: o frasco cadastrado já aberto não fabrica tara nem saldo inicial.
    // Não se converte estimativa visual ou informação não aferida em verdade física.
    // A tara real somente nasce do esgotamento medido (peso do recipiente vazio).
    const conteudoNominal = dados.conteudoNominal ?? null; // NULL significa desconhecido; nunca 0
    const pesoVazio: number | null = null;
    const saldoDesconhecido = true;
    // Validade efetiva quando a abertura histórica é desconhecida: não é possível
    // calcular data_abertura + validade_apos_aberto_dias, então a validade exata fica desconhecida.
    // Isso não implica vencido=false: outra informação (ex.: validade_fechado já expirada)
    // pode provar vencimento mesmo sem a data exata.
    const validadeDesconhecida =
      Boolean(dados.validadeDesconhecida) || Boolean(dados.aberturaHistoricaDesconhecida);

    const { novoCodigo } = await obterProximoCodigoFrascoTx(tx);
    const frascoRef = admin.firestore().collection("Frasco_Reagente").doc();

    tx.set(frascoRef, {
      id_almoxarifado: dados.idAlmoxarifado,
      id_lote: dados.idLote ?? null,
      id_especificacao_reagente: dados.idEspecificacaoReagente,
      id_resumo_reagente: dados.idResumoReagente,
      eh_higroscopico: resumo.eh_higroscopico,
      codigo_frasco: novoCodigo,
      conteudo_nominal: conteudoNominal,
      peso_no_cadastrado: dados.pesoTotalBalanca,
      peso_atual: dados.pesoTotalBalanca,
      peso_frasco_vazio: pesoVazio, // NULL: tara do recipiente ainda não conhecida
      medida_usada: 0,
      condicao_inicial_cadastro: "JA_ABERTO", // dimensão histórica imutável
      data_abertura: dados.aberturaHistoricaDesconhecida ? null : dados.dataAbertura,
      abertura_historica_desconhecida: dados.aberturaHistoricaDesconhecida,
      saldo_desconhecido: saldoDesconhecido,
      estado_fisico_frasco: "ABERTO",
      disponibilidade: "DISPONIVEL",
      validade_efetiva: validadeDesconhecida ? null : validadeAbertoStr,
      validade_desconhecida: validadeDesconhecida,
      vencido: jaVencido,
      em_quarentena: jaVencido && dados.decisaoSeJaVencido === "QUARENTENA",
      uso_vencido_autorizado: jaVencido && dados.decisaoSeJaVencido === "DISPONIVEL",
      detalhe_status: jaVencido
        ? (dados.detalheStatus ?? (dados.decisaoSeJaVencido === "PENDENTE_DE_DESCARTE"
            ? "Frasco vencido aguardando processo institucional de descarte." : null))
        : null,
      cadastrado_em: admin.firestore.FieldValue.serverTimestamp(),
      cadastrado_por: request.auth!.uid,
    });

    if (dados.idLote) {
      tx.set(admin.firestore().collection("Lote_Materializado").doc(dados.idLote), {
        qtd\_frascos\_cadastrados: admin.firestore.FieldValue.increment(1)
      }, { merge: true });
    }

    const historicoRef = admin.firestore().collection("Historico_Frasco_Reagente").doc();
    tx.set(historicoRef, {
      id_frasco_reagente: frascoRef.id,
      id_almoxarifado: dados.idAlmoxarifado,
      id_gestor: request.auth!.uid,
      tipo: "CADASTRO",
      timestamp: admin.firestore.FieldValue.serverTimestamp(),
    });

    const resultado = { idFrasco: frascoRef.id, codigoFrasco: novoCodigo, conteudoNominal, pesoVazio, venceuNoCadastro: jaVencido };
    
    tx.set(operacaoRef, {
      tipo_operacao: "CADASTRAR_ABERTO",
      uid: request.auth!.uid,
      payload_hash: payloadHash,
      resultado,
      status: "CONCLUIDA",
      criado_em: admin.firestore.FieldValue.serverTimestamp(),
      atualizado_em: admin.firestore.FieldValue.serverTimestamp()
    });

    return resultado;
  });
});
\end{lstlisting}

\begin{lstlisting}[language=TypeScript, title={Abertura e cálculo de validade efetiva no servidor}]
function calcularValidadeEfetivaNaAbertura(frasco: any, dataAbertura: Date): Date | null {
  if (frasco.validade_desconhecida) return null;

  const validadeFechado = frasco.validade_fechado
    ? (typeof frasco.validade_fechado.toDate === "function"
      ? frasco.validade_fechado.toDate()
      : DateTime.fromISO(frasco.validade\_fechado, { zone: "America/Sao_Paulo" }).endOf("day").toJSDate())
    : null;
  const diasDepoisDeAberto = frasco.validade_apos_aberto_dias;
  const validadeDepoisDaAbertura = diasDepoisDeAberto != null
    ? DateTime.fromJSDate(new Date(dataAbertura.getTime() + diasDepoisDeAberto * 24 * 60 * 60 * 1000), { zone: "America/Sao_Paulo" }).endOf("day").toJSDate()
    : null;

  if (validadeFechado && validadeDepoisDaAbertura) {
    return validadeFechado <= validadeDepoisDaAbertura ? validadeFechado : validadeDepoisDaAbertura;
  }
  return validadeFechado ?? validadeDepoisDaAbertura;
}

interface AberturaFrasco {
  idFrasco: string;
  destinoSeVencerNaAbertura?: "QUARENTENA" | "PENDENTE_DE_DESCARTE" | "DISPONIVEL";
  detalheStatus?: string;
}

export const registrarAberturaFrasco = onCall(async (request) => {
  const dados = request.data as AberturaFrasco;
  await validarPermissao(request, ["Chefe_Geral", "Gestor_Almoxarifado"]);

  const frascoRef = admin.firestore().collection("Frasco_Reagente").doc(dados.idFrasco);
  return admin.firestore().runTransaction(async (tx) => {
    const snap = await tx.get(frascoRef);
    if (!snap.exists) throw new HttpsError("not-found", "Frasco não encontrado.");
    const frasco = snap.data()!;
    await validarGestorDoAlmoxarifadoTx(tx, request.auth!.uid, frasco.id_almoxarifado);

    if (frasco.estado_fisico_frasco !== "FECHADO") {
      throw new HttpsError("failed-precondition", "Somente um frasco FECHADO pode ser aberto por esta operação.");
    }

    const agora = new Date();
    const validadeEfetiva = calcularValidadeEfetivaNaAbertura(frasco, agora);
    const venceu = Boolean(validadeEfetiva && validadeEfetiva <= agora);

    if (venceu && !dados.destinoSeVencerNaAbertura) {
      throw new HttpsError("failed-precondition", "A abertura tornou o frasco vencido. O gestor deve escolher o destino do frasco.");
    }
    if (venceu && dados.destinoSeVencerNaAbertura === "QUARENTENA") {
      validarJustificativaHumana(dados.detalheStatus);
    }

    tx.update(frascoRef, {
      estado_fisico_frasco: "ABERTO",
      data_abertura: DateTime.fromJSDate(agora, { zone: "America/Sao_Paulo" }).toISODate(),
      validade_efetiva: validadeEfetiva ? DateTime.fromJSDate(validadeEfetiva, { zone: "America/Sao_Paulo" }).toISODate() : null,
      vencido: venceu,
      em_quarentena: venceu && dados.destinoSeVencerNaAbertura === "QUARENTENA",
      uso_vencido_autorizado: venceu && dados.destinoSeVencerNaAbertura === "DISPONIVEL",
      detalhe_status: venceu
        ? (dados.detalheStatus ?? (dados.destinoSeVencerNaAbertura === "PENDENTE_DE_DESCARTE"
          ? "Frasco vencido aguardando processo institucional de descarte."
          : null))
        : null,
    });

    // MET-07: registro transacional do evento de abertura no histórico
    tx.set(admin.firestore().collection("Historico_Frasco_Reagente").doc(), {
      id_frasco_reagente: frascoRef.id,
      id_almoxarifado: frasco.id_almoxarifado,
      id_gestor: request.auth!.uid,
      tipo: !venceu
        ? "AJUSTE"
        : dados.destinoSeVencerNaAbertura === "QUARENTENA"
          ? "ENTROU_EM_QUARENTENA"
          : dados.destinoSeVencerNaAbertura === "DISPONIVEL"
            ? "USO_VENCIDO_AUTORIZADO"
            : "PENDENTE_DE_DESCARTE",
      ...(venceu ? {} : { campo_ajustado: "data_abertura" }),
      motivo: "Primeira abertura física do recipiente.",
      timestamp: admin.firestore.FieldValue.serverTimestamp(),
    });

    return { validadeEfetiva, venceu, destino: dados.destinoSeVencerNaAbertura ?? null };
  });
});

\end{lstlisting}

\begin{lstlisting}[language=TypeScript, title={Registrar Retirada (Empréstimo)}]
interface RetiradaFrasco {
  idFrasco: string;
  idUsuarioRetirou: string;
  idLocalUsado: string;
  pesoSaida: number;
  dataDevolucaoPrevista: string;
  confirmarUsoVencido?: boolean;
  abrirNoEmprestimo?: boolean;
  finalidadeUso: "AULA_PRATICA" | "DEMONSTRACAO" | "PESQUISA_TCC_POS" | "ESTUDO_DEGRADACAO_RESIDUOS";
  justificativaMetodologica?: string;
  idAceiteTcr?: string; // evento prévio autenticado pelo retirante
  justificativaAutoAtendimento?: string;
}

export const registrarRetirada = onCall(async (request) => {
  const dados = request.data as RetiradaFrasco;
  await validarPermissao(request, ["Chefe_Geral", "Gestor_Almoxarifado"], true);

  // M-05: verificar conta ativa do tomador (Professor ou Bolsista)
  // Custom Claims não estão disponíveis para terceiros; leitura Firestore necessária (1 ou 2 leituras).
  const [profSnap, bolsSnap, tomadorSnap, chefeTomadorSnap] = await Promise.all([
    admin.firestore().collection("Professor").doc(dados.idUsuarioRetirou).get(),
    admin.firestore().collection("Bolsista").doc(dados.idUsuarioRetirou).get(),
    admin.firestore().collection("Usuarios").doc(dados.idUsuarioRetirou).get(),
    admin.firestore().collection("Chefe_Geral").doc(dados.idUsuarioRetirou).get(),
  ]);
  if (chefeTomadorSnap.exists || (!profSnap.exists && !bolsSnap.exists)) {
    throw new HttpsError("failed-precondition", "Retirante deve ser Professor ou Bolsista e não pode ser Chefe Geral.");
  }
  if (!tomadorSnap.exists || tomadorSnap.data()?.ativo !== true) {
    throw new HttpsError("failed-precondition", "Conta do tomador está desativada ou não encontrada.");
  }

  const frascoRef = admin.firestore().collection("Frasco_Reagente").doc(dados.idFrasco);

  return admin.firestore().runTransaction(async (tx) => {
    const frascoSnap = await tx.get(frascoRef);
    if (!frascoSnap.exists) throw new HttpsError("not-found", "Frasco não encontrado.");
    const frasco = frascoSnap.data()!;

    await validarGestorDoAlmoxarifadoTx(tx, request.auth!.uid, frasco.id_almoxarifado);
    if (frasco.disponibilidade !== "DISPONIVEL") {
      throw new HttpsError("failed-precondition", "Frasco não está disponível para retirada.");
    }
    if (frasco.estado_fisico_frasco !== "FECHADO" && frasco.estado_fisico_frasco !== "ABERTO") {
      throw new HttpsError("failed-precondition", "Estado físico incompatível com retirada.");
    }
    if (frasco.em_quarentena) {
      throw new HttpsError("failed-precondition", "Frasco em quarentena não pode ser retirado.");
    }

    const agora = new Date();
    const { DateTime } = require("luxon");
    let validadeEfetiva = frasco.validade_efetiva
      ? (typeof frasco.validade_efetiva.toDate === "function"
        ? frasco.validade_efetiva.toDate()
        : DateTime.fromISO(frasco.validade_efetiva, { zone: "America/Sao_Paulo" }).endOf("day").toJSDate())
      : null;
    let frascoVencido = Boolean(validadeEfetiva && validadeEfetiva <= agora);

    if (dados.abrirNoEmprestimo && frasco.estado_fisico_frasco === "FECHADO") {
      validadeEfetiva = calcularValidadeEfetivaNaAbertura(frasco, agora);
      frascoVencido = Boolean(validadeEfetiva && validadeEfetiva <= agora);
    }

    const excepcional = frascoVencido || frasco.validade_desconhecida;
    if (excepcional && (!frasco.uso_vencido_autorizado || !dados.confirmarUsoVencido))
      throw new HttpsError("failed-precondition", "Uso excepcional exige autorização e ciência.");
    const finalidade = dados.finalidadeUso; // nunca substituir finalidade declarada
    const pesquisa = ["PESQUISA_TCC_POS", "ESTUDO_DEGRADACAO_RESIDUOS"].includes(finalidade);
    // Helpers contratuais: todas as leituras devem ocorrer antes da primeira escrita.
    // Aceite foi criado por endpoint autenticado do retirante, não pelo gestor.
    let aceite = null;
    if (excepcional && pesquisa) {
      validarJustificativaHumana(dados.justificativaMetodologica);
      // Q04/Q14: o aceite é sempre emitido em sessão autenticada do retirante; o gestor nunca o declara em nome de terceiro.
      aceite = await validarAceiteTcrTx(tx, dados.idAceiteTcr, {
        retirante: dados.idUsuarioRetirou, frasco: dados.idFrasco,
        finalidade, justificativa: dados.justificativaMetodologica,
      });
    }
    // O helper exige validarJustificativaHumana quando caracteriza autoatendimento (Q14).
    const autoAtendimento = await validarAutoAtendimentoTx(tx, request.auth!.uid,
      dados.idUsuarioRetirou, frasco.id_almoxarifado, dados.justificativaAutoAtendimento);
    const quimica = await resolverSnapshotQuimicoTx(tx, frasco);

    const emprestimoRef = admin.firestore().collection("Emprestimo_Reagente").doc();
    tx.set(emprestimoRef, {
      id_frasco_reagente: dados.idFrasco,
      // M-20: id_usuario_retirou = o portador físico (Professor ou Bolsista que vai usar o frasco)
      // id_gestor_retirada = o operador do almoxarifado que registrou a operação (uid do requisitante)
      id_usuario_retirou: dados.idUsuarioRetirou,
      id_gestor_retirada: request.auth!.uid,
      id_almoxarifado: frasco.id_almoxarifado,
      status: "EM_USO",
      data_retirada: admin.firestore.FieldValue.serverTimestamp(),
      data\_devolucao\_prevista: DateTime.fromISO(dados.dataDevolucaoPrevista, { zone: "America/Sao_Paulo" }).endOf("day").toJSDate(),
      peso\_perda\_evaporacao: 0,
      id\_local\_usado: dados.idLocalUsado,
      peso_saida: dados.pesoSaida,
      uso_vencido_aceito: Boolean(frascoVencido),
      finalidade_uso: finalidade,
      auto_atendimento: autoAtendimento,
      justificativa_metodologica: dados.justificativaMetodologica ?? null,
      tcr_versao: aceite?.versao ?? null,
      tcr_aceito_em: aceite?.aceito_em ?? null,
      tcr_aceito_por: aceite?.uid ?? null,
      tcr_auditoria_id: aceite?.auditoria_id ?? null,
      unidade_medida_utilizada: quimica.unidade,
      densidade_aplicada: quimica.densidade,
    });

    tx.update(frascoRef, {
      disponibilidade: "EMPRESTADO",
      ...(dados.abrirNoEmprestimo && frasco.estado_fisico_frasco === "FECHADO"
        ? { estado_fisico_frasco: "ABERTO", data_abertura: DateTime.fromJSDate(agora, { zone: "America/Sao_Paulo" }).toISODate(),
            validade_efetiva: validadeEfetiva ? DateTime.fromJSDate(validadeEfetiva, { zone: "America/Sao_Paulo" }).toISODate() : null,
            vencido: frascoVencido } : {}),
    });
    if (aceite) vincularAceiteEAuditoriaTx(tx, aceite, emprestimoRef.id);
    if (autoAtendimento) registrarNotificacaoChefiaTx(tx, emprestimoRef.id);

    const historicoRef = admin.firestore().collection("Historico_Frasco_Reagente").doc();
    tx.set(historicoRef, {
      id_frasco_reagente: dados.idFrasco,
      id_almoxarifado: frasco.id_almoxarifado,
      id_gestor: request.auth!.uid,
      tipo: "SAIU",
      id_emprestimo_reagente: emprestimoRef.id,
      timestamp: admin.firestore.FieldValue.serverTimestamp(),
    });

    return { idEmprestimo: emprestimoRef.id };
  });
});
\end{lstlisting}

\begin{lstlisting}[language=TypeScript, title={Registrar Devolução com atualização física e histórico completo}]
interface DevolucaoFrasco {
  idEmprestimo: string;
  pesoRetorno: number;
  pesoPerdaEvaporacao?: number;
  // "O frasco ficou vazio nesta devolução?" - autoridade do gestor, válida para
  // frascos originalmente FECHADOS e para frascos cadastrados JA_ABERTO.
  confirmarEsgotamento?: boolean;
  idUsuarioDevolveu: string; // Portador físico que entrega o frasco ao almoxarifado
  destinoPosDevolucao?: "QUARENTENA" | "PENDENTE_DE_DESCARTE" | "DISPONIVEL";
}

export const registrarDevolucao = onCall(async (request) => {
  const dados = request.data as DevolucaoFrasco;
  await validarPermissao(request, ["Chefe_Geral", "Gestor_Almoxarifado"], true);

  const emprestimoRef = admin.firestore().collection("Emprestimo_Reagente").doc(dados.idEmprestimo);

  return admin.firestore().runTransaction(async (tx) => {
    const emprestimoSnap = await tx.get(emprestimoRef);
    if (!emprestimoSnap.exists) throw new HttpsError("not-found", "Empréstimo não encontrado.");
    const emprestimo = emprestimoSnap.data()!;
    if (!["EM_USO", "ATRASADO"].includes(emprestimo.status)) {
      throw new HttpsError("failed-precondition", "Empréstimo já foi devolvido.");
    }

    const frascoRef = admin.firestore().collection("Frasco_Reagente").doc(emprestimo.id_frasco_reagente);
    const frascoSnap = await tx.get(frascoRef);
    if (!frascoSnap.exists) throw new HttpsError("not-found", "Frasco não encontrado.");
    const frasco = frascoSnap.data()!;
    await validarGestorDoAlmoxarifadoTx(tx, request.auth!.uid, frasco.id_almoxarifado);

    const liquido = emprestimo.unidade_medida_utilizada === "ml";
    const densidade = liquido ? emprestimo.densidade_aplicada : null;
    if (liquido && (!Number.isFinite(densidade) || densidade <= 0))
      throw new HttpsError("failed-precondition", "Densidade histórica inválida.");
    const perdaEvaporacao = Number.isFinite(dados.pesoPerdaEvaporacao) ? Math.max(0, dados.pesoPerdaEvaporacao!) : 0;
    let pesoConsumido = Math.max(0, emprestimo.peso_saida - dados.pesoRetorno - perdaEvaporacao);

    if (typeof frasco.eh_higroscopico !== "boolean")
      throw new HttpsError("failed-precondition", "Snapshot histórico ausente: reconciliar frasco.");

    // PDF-025: FORMULA CANONICA Q06
    // A margem de tolerância é estritamente calculada sobre o PESO BRUTO (peso_saida).
    // Rejeitada explicitamente a sugestão de calcular sobre massa líquida,
    // pois a variância ocorre sobre a medição total na balança.
    // Fórmula:
    // Normal: max(1g, 0.5% peso_saida)
    // Higroscópico: max(2g, 2.0% peso_saida)

    const deltaMax = frasco.eh_higroscopico
      ? Math.max(2.0, 0.02 * emprestimo.peso_saida)
      : Math.max(1.0, 0.005 * emprestimo.peso_saida);
    const limiteMaxRetorno = emprestimo.peso_saida + deltaMax;
    if (!Number.isFinite(dados.pesoRetorno) || dados.pesoRetorno <= 0) {
      throw new HttpsError("invalid-argument", "Peso de retorno inválido.");
    }
    // M2.1b / Q06: o esgotamento é decisão explícita do gestor, não consequência
    // automática da comparação com a tara. A tolerância de pesagem permanece
    // apenas como sinal de anomalia/justificativa, nunca como autoridade de vazio.
    const ficouVazio = dados.confirmarEsgotamento === true;
    const pesoVazioReferencia = frasco.peso_frasco_vazio; // NULL para frasco cadastrado JA_ABERTO
    // Evita consumo negativo: retorno abaixo da tara exige confirmação explícita de vazio.
    if (!ficouVazio && pesoVazioReferencia != null && dados.pesoRetorno < pesoVazioReferencia) {
      throw new HttpsError("failed-precondition", "Retorno abaixo da tara exige a confirmação explícita de que o frasco ficou vazio.");
    }
    if (ficouVazio) pesoConsumido = Math.max(0, emprestimo.peso_saida - dados.pesoRetorno);
    // PDF-025: Ganho acima da tolerância bloqueia por anomalia.
    let reterPorAnomalia = false;
    if (dados.pesoRetorno > limiteMaxRetorno) {
      reterPorAnomalia = true;
    }
    const ganhoMassa = dados.pesoRetorno > emprestimo.peso_saida;
    const avisoHigroscopico = ganhoMassa && frasco.eh_higroscopico === true && !reterPorAnomalia;
    const volumeUtilizado = densidade ? pesoConsumido / densidade : pesoConsumido;

    const agora = new Date();
    // DP-D02: Normalização via Luxon (America/Sao_Paulo). Prazo encerra-se às 23:59:59.999 do dia previsto.
    const { DateTime } = require("luxon");
    const prazoLimite = DateTime.fromJSDate(emprestimo.data_devolucao_prevista.toDate(), { zone: "America/Sao_Paulo" })
      .endOf("day").toJSDate();
    const atrasado = agora > prazoLimite;

    const validadeEfetiva = frasco.validade_efetiva
      ? (typeof frasco.validade_efetiva.toDate === "function"
        ? frasco.validade_efetiva.toDate()
        : DateTime.fromISO(frasco.validade_efetiva, { zone: "America/Sao_Paulo" }).endOf("day").toJSDate())
      : null;
    const venceuAgora = Boolean(validadeEfetiva && validadeEfetiva <= agora);
    const frascoVencido = Boolean(frasco.vencido || venceuAgora);

    const atualizacaoFrasco: Record<string, unknown> = {
      disponibilidade: ficouVazio || reterPorAnomalia ? "INDISPONIVEL" : "DISPONIVEL",
      ...(ficouVazio && {
        estado_fisico_frasco: "VAZIO",
        peso_frasco_vazio: dados.pesoRetorno, // tara real: recipiente efetivamente vazio medido na balança
        saldo_desconhecido: false,            // saldo passa a ser 0 g / 0 mL conhecido
        peso_atual: dados.pesoRetorno
      }),
      ...(!ficouVazio && { peso_atual: dados.pesoRetorno }),
      data_ultima_pesagem: admin.firestore.FieldValue.serverTimestamp(),
      medida_usada: admin.firestore.FieldValue.increment(pesoConsumido), // sempre g
      vencido: frascoVencido, // derivado das regras reais de validade; nunca forçado para bloquear
    };

    if (reterPorAnomalia) {
      atualizacaoFrasco.em_quarentena = true;
      atualizacaoFrasco.uso_vencido_autorizado = false;
      atualizacaoFrasco.detalhe_status = "Frasco retido por anomalia de pesagem na devolução.";
    } else if (frascoVencido && dados.destinoPosDevolucao === "QUARENTENA") {
      atualizacaoFrasco.em_quarentena = true;
      atualizacaoFrasco.uso_vencido_autorizado = false;
      atualizacaoFrasco.detalhe_status = "Frasco vencido retido em quarentena após devolução.";
    } else if (frascoVencido && dados.destinoPosDevolucao === "DISPONIVEL") {
      atualizacaoFrasco.em_quarentena = false;
      atualizacaoFrasco.uso_vencido_autorizado = true;
    } else if (frascoVencido && dados.destinoPosDevolucao === "PENDENTE_DE_DESCARTE") {
      atualizacaoFrasco.em_quarentena = false;
      atualizacaoFrasco.uso_vencido_autorizado = false;
      atualizacaoFrasco.detalhe_status = "Frasco vencido aguardando processo institucional de descarte.";
    }

    // MET-07: Emissão obrigatória do evento de domínio correspondente ao destino do frasco vencido
    if (!reterPorAnomalia && frascoVencido) {
      const tipoDestino = dados.destinoPosDevolucao === "QUARENTENA"
        ? "ENTROU_EM_QUARENTENA"
        : dados.destinoPosDevolucao === "DISPONIVEL"
          ? "USO_VENCIDO_AUTORIZADO"
          : dados.destinoPosDevolucao === "PENDENTE_DE_DESCARTE"
            ? "PENDENTE_DE_DESCARTE"
            : null;
      if (tipoDestino) {
        tx.set(admin.firestore().collection("Historico_Frasco_Reagente").doc(), {
          id_frasco_reagente: emprestimo.id_frasco_reagente,
          id_almoxarifado: frasco.id_almoxarifado,
          id_gestor: request.auth!.uid,
          id_emprestimo_reagente: emprestimoRef.id,
          tipo: tipoDestino,
          timestamp: admin.firestore.FieldValue.serverTimestamp(),
        });
      }
    }

    tx.update(emprestimoRef, {
      status: reterPorAnomalia ? "DEVOLVIDO_COM_ANOMALIA" : (atrasado ? "DEVOLVIDO_COM_ATRASO" : "DEVOLVIDO"),
      data_devolucao_efetuada: admin.firestore.FieldValue.serverTimestamp(),
      id_usuario_devolveu: dados.idUsuarioDevolveu,
      id_gestor_devolucao: request.auth!.uid,
      peso_retorno: dados.pesoRetorno,
      peso_perda_evaporacao: dados.pesoPerdaEvaporacao || 0,
      medida_utilizada: volumeUtilizado,
      unidade_medida_utilizada: densidade ? "ml" : "g",
    ...(avisoHigroscopico && { aviso: "higroscopico_suspeito" }),
    });
    tx.update(frascoRef, atualizacaoFrasco);

    // M2.1b: não existe recalibração parcial de tara a partir de estimativa com
    // produto restante. A única recalibração válida é a tara real medida com o
    // recipiente efetivamente vazio, registrada em recalibrarTaraFrascoEsgotado.

    // MET-02: Retenção por anomalia registra compulsoriamente a entrada em quarentena
    if (reterPorAnomalia) {
      tx.set(admin.firestore().collection("Historico_Frasco_Reagente").doc(), {
        id_frasco_reagente: frascoRef.id,
        id_almoxarifado: frasco.id_almoxarifado,
        id_gestor: request.auth!.uid,
        id_emprestimo_reagente: emprestimoRef.id,
        tipo: "ENTROU_EM_QUARENTENA",
        motivo: "Retido na devolução: Ganho de massa (" + (dados.pesoRetorno - emprestimo.peso_saida) + "g) excedeu a tolerância legal Q06.",
        timestamp: admin.firestore.FieldValue.serverTimestamp(),
      });
    }

    // MET-02: Histórico de ganho higroscópico só para frascos comprovadamente higroscópicos
    if (avisoHigroscopico && !reterPorAnomalia && frasco.eh_higroscopico === true) {
      tx.set(admin.firestore().collection("Historico_Frasco_Reagente").doc(), {
        id_frasco_reagente: frascoRef.id,
        id_almoxarifado: frasco.id_almoxarifado,
        id_gestor: request.auth!.uid,
        id_emprestimo_reagente: emprestimoRef.id,
        tipo: "AJUSTE", campo_ajustado: "ganho_massa_higroscopia",
        peso_anterior: emprestimo.peso_saida, peso_novo: dados.pesoRetorno,
        medida_ajustada: dados.pesoRetorno - emprestimo.peso_saida,
        unidade_medida_ajustada: "g",
        timestamp: admin.firestore.FieldValue.serverTimestamp(),
      });
    }

    if (ficouVazio) {
      tx.set(admin.firestore().collection("Historico_Frasco_Reagente").doc(), {
        id_frasco_reagente: frascoRef.id, id_almoxarifado: frasco.id_almoxarifado,
        id_gestor: request.auth!.uid, id_emprestimo_reagente: emprestimoRef.id,
        tipo: "FICOU_VAZIO", campo_ajustado: "tara_real_homologada",
        peso_medido: dados.pesoRetorno, tara_aplicada: dados.pesoRetorno,
        peso_anterior: frasco.peso_frasco_vazio,
        confirmado_esgotamento: true, timestamp: admin.firestore.FieldValue.serverTimestamp(),
      });
    }

    if (perdaEvaporacao > 0) {
      tx.set(admin.firestore().collection("Historico_Frasco_Reagente").doc(), {
        id_frasco_reagente: emprestimo.id_frasco_reagente,
        id_almoxarifado: frasco.id_almoxarifado,
        id_gestor: request.auth!.uid,
        tipo: "EVAPORACAO",
        id_emprestimo_reagente: emprestimoRef.id,
        medida_ajustada: perdaEvaporacao,
        unidade_medida_ajustada: "g",
        timestamp: admin.firestore.FieldValue.serverTimestamp(),
      });
    }

    // Registro obrigatório de devolução no histórico
    const histRef = admin.firestore().collection("Historico_Frasco_Reagente").doc();
    tx.set(histRef, {
      id_frasco_reagente: emprestimo.id_frasco_reagente,
      id_almoxarifado: frasco.id_almoxarifado,
      id_gestor: request.auth!.uid,
      tipo: "ENTROU",
      id_emprestimo_reagente: emprestimoRef.id,
      peso_anterior: emprestimo.peso_saida,
      peso_novo: dados.pesoRetorno,
      medida_ajustada: volumeUtilizado,
      unidade_medida_ajustada: densidade ? "ml" : "g",
      timestamp: admin.firestore.FieldValue.serverTimestamp(),
    });

    return { pesoConsumido, volumeUtilizado, atrasado, frascoVencido,
           avisoHigroscopico: avisoHigroscopico || false };
  });
});

interface StatusExtraordinario {
  idFrasco: string;
  acao: "EXTRAVIAR" | "REENCONTRAR";
  motivo: string;
  estadoConstatadoAoReencontrar?: "ABERTO" | "FECHADO"; // obrigatório se REENCONTRAR
  id_operacao: string;
}

export const registrarExtravioOuReencontro = onCall(async (request) => {
  const dados = request.data as StatusExtraordinario;
  await validarPermissao(request, ["Chefe_Geral", "Gestor_Almoxarifado"], true);
  if (!dados.id_operacao) throw new HttpsError("invalid-argument", "id_operacao é obrigatório.");

  const frascoRef = admin.firestore().collection("Frasco_Reagente").doc(dados.idFrasco);
  const operacaoRef = admin.firestore().collection("Operacoes").doc(dados.id_operacao);

  return admin.firestore().runTransaction(async (tx) => {
    const crypto = require("crypto");
    const payloadRelevante = dados.acao === "EXTRAVIAR"
      ? { acao: dados.acao, idFrasco: dados.idFrasco, motivo: dados.motivo }
      : { acao: dados.acao, idFrasco: dados.idFrasco, motivo: dados.motivo, estadoConstatadoAoReencontrar: dados.estadoConstatadoAoReencontrar };
    const payloadStr = JSON.stringify(payloadRelevante, Object.keys(payloadRelevante).sort());
    const payloadHash = crypto.createHash("sha256").update(payloadStr).digest("hex");

    // 1. ler registro de idempotência
    const opSnap = await tx.get(operacaoRef);
    if (opSnap.exists) {
      const op = opSnap.data()!;
      if (op.uid === request.auth!.uid && op.payload_hash === payloadHash) {
        return op.resultado;
      }
      throw new HttpsError("already-exists", "id_operacao duplicada: payload_hash ou ator divergente.");
    }

    // 2. ler frasco
    validarJustificativaHumana(dados.motivo);
    const frascoSnap = await tx.get(frascoRef);
    if (!frascoSnap.exists) throw new HttpsError("not-found", "Frasco não encontrado.");
    const frasco = frascoSnap.data()!;

    // 3. validar versão/estado e vínculo
    await validarGestorDoAlmoxarifadoTx(tx, request.auth!.uid, frasco.id_almoxarifado);

    // 4. ler eventual empréstimo EM_USO|ATRASADO (se for EXTRAVIAR)
    if (dados.acao === "EXTRAVIAR") {
      if (frasco.estado_fisico_frasco === "EXTRAVIADO")
        throw new HttpsError("failed-precondition", "O frasco já consta como extraviado.");

      const queryEmprestimo = admin.firestore().collection("Emprestimo_Reagente")
        .where("id_frasco_reagente", "==", dados.idFrasco)
        .where("status", "in", ["EM_USO", "ATRASADO"])
        .limit(1);
      const emprestimoAberto = await tx.get(queryEmprestimo);

      tx.update(frascoRef, {
        estado\_fisico\_frasco: "EXTRAVIADO",
        disponibilidade: "INDISPONIVEL",
        detalhe\_status: dados.motivo,
      });

      if (!emprestimoAberto.empty) {
        const docEmprestimo = emprestimoAberto.docs[0];
        if (docEmprestimo.data().peso_retorno != null) {
          throw new HttpsError("failed-precondition", "Empréstimo já possui medição de peso_retorno. Reconciliação manual necessária.");
        }
        const pesoSaida = docEmprestimo.data().peso_saida || 0;
        const pesoVazio = frasco.peso_frasco_vazio != null ? frasco.peso_frasco_vazio : null;
        const perdaEstimada = pesoVazio != null ? Math.max(0, pesoSaida - pesoVazio) : pesoSaida;

        tx.update(docEmprestimo.ref, {
          status: "ENCERRADO_EXTRAORDINARIO",
          tipo_encerramento_excepcional: "EXTRAVIO_SINISTRO",
          motivo_encerramento_excepcional: dados.motivo,
          id_gestor_encerramento: request.auth!.uid,
          massa_perda_estimada_g: perdaEstimada,
          peso_retorno: null,
          medida_utilizada: 0,
          data_devolucao_efetuada: admin.firestore.FieldValue.serverTimestamp(),
        });
      }

      tx.set(admin.firestore().collection("Historico_Frasco_Reagente").doc(), {
        id_frasco_reagente: dados.idFrasco,
        id_almoxarifado: frasco.id_almoxarifado,
        id_gestor: request.auth!.uid,
        tipo: "EXTRAVIO",
        motivo: dados.motivo,
        timestamp: admin.firestore.FieldValue.serverTimestamp(),
      });
    } else { // REENCONTRAR
      if (frasco.estado_fisico_frasco !== "EXTRAVIADO")
        throw new HttpsError("failed-precondition", "Frasco não está extraviado.");
      if (!dados.estadoConstatadoAoReencontrar)
        throw new HttpsError("invalid-argument", "Estado constatado é obrigatório.");

      // M2.1b: reencontro impõe quarentena e INDISPONIVEL e não fabrica data de abertura.
      const agora = new Date();
      const { DateTime } = require("luxon");

      let validadeCalculada = frasco.validade_efetiva
        ? (typeof frasco.validade_efetiva.toDate === "function"
          ? frasco.validade_efetiva.toDate()
          : DateTime.fromISO(frasco.validade_efetiva, { zone: "America/Sao_Paulo" }).endOf("day").toJSDate())
        : null;

      // Só era FECHADO se nunca houve abertura conhecida. Se for encontrado ABERTO
      // e a data da primeira abertura for indeterminável, a flag histórica é marcada
      // e a validade efetiva exata passa a ser desconhecida (D9/D9b). Não inventar data.
      const eraFechadoAntes = frasco.data_abertura == null && frasco.abertura_historica_desconhecida !== true;
      const encontradoAbertoSemData = dados.estadoConstatadoAoReencontrar === "ABERTO" && eraFechadoAntes;
      if (encontradoAbertoSemData) {
        validadeCalculada = null; // validade efetiva indeterminável sem a data de abertura
      }

      const venceuAgora = Boolean(validadeCalculada && validadeCalculada <= agora);
      const frascoVencido = Boolean(frasco.vencido || venceuAgora);

      tx.update(frascoRef, {
        estado_fisico_frasco: dados.estadoConstatadoAoReencontrar,
        disponibilidade: "INDISPONIVEL", // quarentena impõe compulsoriamente INDISPONIVEL
        em_quarentena: true,
        ...(encontradoAbertoSemData && {
          abertura_historica_desconhecida: true,
          data_abertura: null,
          validade_desconhecida: true,
        }),
        validade_efetiva: validadeCalculada ? DateTime.fromJSDate(validadeCalculada, { zone: "America/Sao_Paulo" }).toISODate() : null,
        vencido: frascoVencido, // derivado das regras reais de validade; nunca forçado pelo reencontro
        detalhe_status: `Reencontrado em quarentena: ${dados.motivo}`,
        // saldo_desconhecido permanece como estava, salvo medição/evento válido posterior
      });

      tx.set(admin.firestore().collection("Historico_Frasco_Reagente").doc(), {
        id_frasco_reagente: dados.idFrasco,
        id_almoxarifado: frasco.id_almoxarifado,
        id_gestor: request.auth!.uid,
        tipo: "REENCONTRO",
        motivo: dados.motivo,
        timestamp: admin.firestore.FieldValue.serverTimestamp(),
      });
    }

    const resultado = { status: "OK" };
    // Grava operação para garantir idempotência
    tx.set(operacaoRef, {
      tipo_operacao: dados.acao,
      uid: request.auth!.uid,
      payload_hash: payloadHash,
      resultado: resultado,
      status: "CONCLUIDA",
      criado_em: admin.firestore.FieldValue.serverTimestamp(),
      atualizado_em: admin.firestore.FieldValue.serverTimestamp()
    });

    return resultado;
  });
});

interface PayloadRecalibrarTara {
  id_frasco: string;
  id_operacao: string;
  peso_vazio_real: number;
  motivo: string;
}

export const recalibrarTaraFrascoEsgotado = onCall(async (request) => {
  const dados = request.data as PayloadRecalibrarTara;
  await validarPermissao(request, ["Chefe_Geral", "Gestor_Almoxarifado"], true);
  if (!dados.id_operacao) throw new HttpsError("invalid-argument", "id_operacao é obrigatório.");

  const frascoRef = admin.firestore().collection("Frasco_Reagente").doc(dados.id_frasco);
  const operacaoRef = admin.firestore().collection("Operacoes").doc(dados.id_operacao);

  return admin.firestore().runTransaction(async (tx) => {
    const crypto = require("crypto");
    const payloadRelevante = { acao: "RECALIBRAR_TARA", id_frasco: dados.id_frasco, peso_vazio_real: dados.peso_vazio_real, motivo: dados.motivo };
    const payloadStr = JSON.stringify(payloadRelevante, Object.keys(payloadRelevante).sort());
    const payloadHash = crypto.createHash("sha256").update(payloadStr).digest("hex");

    const opSnap = await tx.get(operacaoRef);
    if (opSnap.exists) {
      const op = opSnap.data()!;
      if (op.uid === request.auth!.uid && op.payload_hash === payloadHash) {
        return op.resultado;
      }
      throw new HttpsError("already-exists", "id_operacao duplicada: payload_hash ou ator divergente.");
    }

    validarJustificativaHumana(dados.motivo);
    const frascoSnap = await tx.get(frascoRef);
    if (!frascoSnap.exists) throw new HttpsError("not-found", "Frasco não encontrado.");
    const frasco = frascoSnap.data()!;
    await validarGestorDoAlmoxarifadoTx(tx, request.auth!.uid, frasco.id_almoxarifado);

    // M2.1b/D4: tara real = peso do recipiente efetivamente vazio medido na balança.
    // Não se recalibra tara com produto restante nem por estimativa.
    if (frasco.estado_fisico_frasco !== "VAZIO" && frasco.estado_fisico_frasco !== "DESCARTADO") {
      throw new HttpsError("failed-precondition", "A tara real só pode ser homologada com o recipiente efetivamente vazio e pesado.");
    }

    tx.update(frascoRef, {
      peso_frasco_vazio: dados.peso_vazio_real,
      peso_atual: dados.peso_vazio_real,
      saldo_desconhecido: false,
    });

    tx.set(admin.firestore().collection("Historico_Frasco_Reagente").doc(), {
      id_frasco_reagente: dados.id_frasco,
      id_almoxarifado: frasco.id_almoxarifado,
      id_gestor: request.auth!.uid,
      tipo: "AJUSTE",
      campo_ajustado: "peso_frasco_vazio",
      peso_anterior: frasco.peso_frasco_vazio || null,
      peso_novo: dados.peso_vazio_real,
      motivo: dados.motivo,
      timestamp: admin.firestore.FieldValue.serverTimestamp(),
    });

    const resultado = { status: "OK" };
    tx.set(operacaoRef, {
      tipo_operacao: "RECALIBRAR_TARA",
      uid: request.auth!.uid,
      payload_hash: payloadHash,
      resultado,
      status: "CONCLUIDA",
      criado_em: admin.firestore.FieldValue.serverTimestamp(),
      atualizado_em: admin.firestore.FieldValue.serverTimestamp()
    });

    return resultado;
  });
});

interface DescarteFrasco {
  idFrasco: string;
  motivo: string;
  idOperacao: string;
}

export const descartarFrasco = onCall(async (request) => {
  const dados = request.data as DescarteFrasco;
  await validarPermissao(request, ["Chefe_Geral", "Gestor_Almoxarifado"], true);
  if (!dados.idOperacao) throw new HttpsError("invalid-argument", "idOperacao é obrigatório.");

  const frascoRef = admin.firestore().collection("Frasco_Reagente").doc(dados.idFrasco);
  const operacaoRef = admin.firestore().collection("Operacoes").doc(dados.idOperacao);

  return admin.firestore().runTransaction(async (tx) => {
    const crypto = require("crypto");
    const payloadStr = JSON.stringify({ idFrasco: dados.idFrasco, motivo: dados.motivo });
    const payloadHash = crypto.createHash("sha256").update(payloadStr).digest("hex");

    const opSnap = await tx.get(operacaoRef);
    if (opSnap.exists) {
      const op = opSnap.data()!;
      if (op.uid === request.auth!.uid && op.payload_hash === payloadHash) {
        return op.resultado;
      }
      throw new HttpsError("already-exists", "idOperacao duplicada ou ator divergente.");
    }

    const frascoSnap = await tx.get(frascoRef);
    if (!frascoSnap.exists) throw new HttpsError("not-found", "Frasco não encontrado.");
    const frasco = frascoSnap.data()!;
    await validarGestorDoAlmoxarifadoTx(tx, request.auth!.uid, frasco.id_almoxarifado);

    if (frasco.disponibilidade === "EMPRESTADO") {
      throw new HttpsError("failed-precondition", "Frasco com empr\'estimo ativo n\~ao pode ser descartado. Registre a devolu\c{c}\~ao ou encerramento extraordin\'ario primeiro.");
    }

    // MET-05: Ampliar elegibilidade para vencidos com uso n\~ao autorizado
    const aptoParaDescarte = frasco.estado_fisico_frasco === "VAZIO" ||
      frasco.estado_fisico_frasco === "QUEBRADO" ||
      (frasco.vencido === true && frasco.uso_vencido_autorizado === false);

    if (!aptoParaDescarte) {
      throw new HttpsError("failed-precondition", "Apenas frascos vazios, quebrados ou vencidos aguardando descarte podem ser descartados institucionalmente.");
    }

    tx.update(frascoRef, {
      estado_fisico_frasco: "DESCARTADO",
      disponibilidade: "INDISPONIVEL",
      em_quarentena: false, // MET-05: descarte conclui e extingue a quarentena
      detalhe_status: "Frasco descartado institucionalmente: " + dados.motivo,
    });

    tx.set(admin.firestore().collection("Historico_Frasco_Reagente").doc(), {
      id_frasco_reagente: dados.idFrasco,
      id_almoxarifado: frasco.id_almoxarifado,
      id_gestor: request.auth!.uid,
      tipo: "FOI_DESCARTADO",
      motivo: dados.motivo,
      timestamp: admin.firestore.FieldValue.serverTimestamp(),
    });

    const resultado = { status: "OK" };
    tx.set(operacaoRef, {
      tipo_operacao: "DESCARTAR_FRASCO",
      uid: request.auth!.uid,
      payload_hash: payloadHash,
      resultado,
      status: "CONCLUIDA",
      criado_em: admin.firestore.FieldValue.serverTimestamp(),
      atualizado_em: admin.firestore.FieldValue.serverTimestamp()
    });

    return resultado;
  });
});
\end{lstlisting}

\begin{lstlisting}[language=TypeScript, title={Quarentena manual: entrada transacional com proteção TOCTOU}]
interface ColocarEmQuarentena {
  idFrasco: string;
  idOperacao: string;
  motivo: string; // justificativa humana obrigatória: trim >= 20
}

export const colocarFrascoEmQuarentena = onCall(async (request) => {
  const dados = request.data as ColocarEmQuarentena;
  await validarPermissao(request, ["Chefe_Geral", "Gestor_Almoxarifado"], true);
  if (!dados.idOperacao) throw new HttpsError("invalid-argument", "idOperacao é obrigatório.");

  const frascoRef = admin.firestore().collection("Frasco_Reagente").doc(dados.idFrasco);
  const operacaoRef = admin.firestore().collection("Operacoes").doc(dados.idOperacao);

  return admin.firestore().runTransaction(async (tx) => {
    const crypto = require("crypto");
    const payloadStr = JSON.stringify({ idFrasco: dados.idFrasco, motivo: dados.motivo });
    const payloadHash = crypto.createHash("sha256").update(payloadStr).digest("hex");

    // Idempotência
    const opSnap = await tx.get(operacaoRef);
    if (opSnap.exists) {
      const op = opSnap.data()!;
      if (op.uid === request.auth!.uid && op.payload_hash === payloadHash) return op.resultado;
      throw new HttpsError("already-exists", "idOperacao duplicada ou ator divergente.");
    }

    validarJustificativaHumana(dados.motivo);

    // Leitura do estado atual DENTRO da transação (TOCTOU)
    const frascoSnap = await tx.get(frascoRef);
    if (!frascoSnap.exists) throw new HttpsError("not-found", "Frasco não encontrado.");
    const frasco = frascoSnap.data()!;
    // Escopo do almoxarifado relido dentro da transação
    await validarGestorDoAlmoxarifadoTx(tx, request.auth!.uid, frasco.id_almoxarifado);

    if (frasco.em_quarentena === true)
      throw new HttpsError("failed-precondition", "Frasco já está em quarentena.");
    if (frasco.disponibilidade === "EMPRESTADO")
      throw new HttpsError("failed-precondition", "Frasco com empréstimo ativo exige devolução ou encerramento extraordinário antes da quarentena.");

    tx.update(frascoRef, {
      em_quarentena: true,
      disponibilidade: "INDISPONIVEL",
      detalhe_status: dados.motivo,
      // Preserva vencido, estado_fisico_frasco, saldo_desconhecido e validade.
    });

    tx.set(admin.firestore().collection("Historico_Frasco_Reagente").doc(), {
      id_frasco_reagente: dados.idFrasco,
      id_almoxarifado: frasco.id_almoxarifado,
      id_gestor: request.auth!.uid,
      tipo: "ENTROU_EM_QUARENTENA",
      motivo: dados.motivo,
      timestamp: admin.firestore.FieldValue.serverTimestamp(),
    });

    const resultado = { status: "OK" };
    tx.set(operacaoRef, {
      tipo_operacao: "COLOCAR_EM_QUARENTENA",
      uid: request.auth!.uid,
      payload_hash: payloadHash,
      resultado,
      status: "CONCLUIDA",
      criado_em: admin.firestore.FieldValue.serverTimestamp(),
      atualizado_em: admin.firestore.FieldValue.serverTimestamp(),
    });
    return resultado;
  });
});
\end{lstlisting}

\begin{lstlisting}[language=TypeScript, title={Saída de quarentena: decisão única, transacional, sem revalidar validade}]
interface ResolverQuarentena {
  idFrasco: string;
  idOperacao: string;
  decisao: "VOLTAR_A_DISPONIVEL" | "PENDENTE_DE_DESCARTE";
  motivo: string; // justificativa/laudo humano obrigatório: trim >= 20
}

export const resolverQuarentenaFrasco = onCall(async (request) => {
  const dados = request.data as ResolverQuarentena;
  await validarPermissao(request, ["Chefe_Geral", "Gestor_Almoxarifado"], true);
  if (!dados.idOperacao) throw new HttpsError("invalid-argument", "idOperacao é obrigatório.");

  const frascoRef = admin.firestore().collection("Frasco_Reagente").doc(dados.idFrasco);
  const operacaoRef = admin.firestore().collection("Operacoes").doc(dados.idOperacao);

  return admin.firestore().runTransaction(async (tx) => {
    const crypto = require("crypto");
    const payloadStr = JSON.stringify({ idFrasco: dados.idFrasco, decisao: dados.decisao, motivo: dados.motivo });
    const payloadHash = crypto.createHash("sha256").update(payloadStr).digest("hex");

    const opSnap = await tx.get(operacaoRef);
    if (opSnap.exists) {
      const op = opSnap.data()!;
      if (op.uid === request.auth!.uid && op.payload_hash === payloadHash) return op.resultado;
      throw new HttpsError("already-exists", "idOperacao duplicada ou ator divergente.");
    }

    validarJustificativaHumana(dados.motivo);

    const frascoSnap = await tx.get(frascoRef);
    if (!frascoSnap.exists) throw new HttpsError("not-found", "Frasco não encontrado.");
    const frasco = frascoSnap.data()!;
    await validarGestorDoAlmoxarifadoTx(tx, request.auth!.uid, frasco.id_almoxarifado);

    // TOCTOU: a decisão concorrente encontra a pré-condição já consumida e é rejeitada.
    if (frasco.em_quarentena !== true)
      throw new HttpsError("failed-precondition", "Frasco não está mais em quarentena.");

    const voltarDisponivel = dados.decisao === "VOLTAR_A_DISPONIVEL";
    // Demais regras independentes continuam aplicáveis: frasco vencido sem uso
    // autorizado não volta a DISPONIVEL por esta decisão.
    if (voltarDisponivel && frasco.vencido === true && frasco.uso_vencido_autorizado !== true) {
      throw new HttpsError("failed-precondition", "Frasco vencido sem uso autorizado não pode voltar a DISPONIVEL; escolha pendência de descarte ou trate a validade pelas regras próprias.");
    }
    tx.update(frascoRef, {
      em_quarentena: false,
      disponibilidade: voltarDisponivel ? "DISPONIVEL" : "INDISPONIVEL",
      detalhe_status: voltarDisponivel
        ? null
        : "Quarentena encerrada: frasco pendente de descarte institucional.",
      // vencido, estado_fisico_frasco, saldo_desconhecido e validade não são alterados.
      // Não existe revalidação de validade: sair da quarentena não renova vencimento.
    });

    tx.set(admin.firestore().collection("Historico_Frasco_Reagente").doc(), {
      id_frasco_reagente: dados.idFrasco,
      id_almoxarifado: frasco.id_almoxarifado,
      id_gestor: request.auth!.uid,
      tipo: "SAIU_DA_QUARENTENA",
      decisao_quarentena: dados.decisao,
      motivo: dados.motivo,
      timestamp: admin.firestore.FieldValue.serverTimestamp(),
    });

    const resultado = { status: "OK", decisao: dados.decisao };
    tx.set(operacaoRef, {
      tipo_operacao: "RESOLVER_QUARENTENA",
      uid: request.auth!.uid,
      payload_hash: payloadHash,
      resultado,
      status: "CONCLUIDA",
      criado_em: admin.firestore.FieldValue.serverTimestamp(),
      atualizado_em: admin.firestore.FieldValue.serverTimestamp(),
    });
    return resultado;
  });
});
\end{lstlisting}

\begin{lstlisting}[language=TypeScript, title={Correção compensatória de erro operacional do gestor}]
interface CorrigirOperacao {
  idOperacao: string;            // nova operação corretora (idempotência)
  idOperacaoCorrigida: string;   // operação original preservada
  tipoCorrecao: "RETIRADA_RETIRANTE_INCORRETO";
  idUsuarioRetirouCorreto?: string; // obrigatório no tipo acima
  motivo: string;                // justificativa humana obrigatória: trim >= 20
}

export const corrigirOperacao = onCall(async (request) => {
  const dados = request.data as CorrigirOperacao;
  await validarPermissao(request, ["Chefe_Geral", "Gestor_Almoxarifado"], true);
  if (!dados.idOperacao || !dados.idOperacaoCorrigida)
    throw new HttpsError("invalid-argument", "idOperacao e idOperacaoCorrigida são obrigatórios.");

  const operacaoRef = admin.firestore().collection("Operacoes").doc(dados.idOperacao);
  const originalRef = admin.firestore().collection("Operacoes").doc(dados.idOperacaoCorrigida);

  return admin.firestore().runTransaction(async (tx) => {
    const crypto = require("crypto");
    const payloadStr = JSON.stringify({
      idOperacaoCorrigida: dados.idOperacaoCorrigida, tipoCorrecao: dados.tipoCorrecao,
      idUsuarioRetirouCorreto: dados.idUsuarioRetirouCorreto, motivo: dados.motivo,
    });
    const payloadHash = crypto.createHash("sha256").update(payloadStr).digest("hex");

    const opSnap = await tx.get(operacaoRef);
    if (opSnap.exists) {
      const op = opSnap.data()!;
      if (op.uid === request.auth!.uid && op.payload_hash === payloadHash) return op.resultado;
      throw new HttpsError("already-exists", "idOperacao duplicada ou ator divergente.");
    }

    validarJustificativaHumana(dados.motivo);

    // A operação original NÃO é apagada; a trilha histórica é preservada.
    const originalSnap = await tx.get(originalRef);
    if (!originalSnap.exists) throw new HttpsError("not-found", "Operação original não encontrada.");
    const original = originalSnap.data()!;

    const emprestimoRef = admin.firestore().collection("Emprestimo_Reagente").doc(original.resultado?.idEmprestimo);
    const emprestimoSnap = await tx.get(emprestimoRef);
    if (!emprestimoSnap.exists) throw new HttpsError("not-found", "Empréstimo da operação original não encontrado.");
    const emprestimo = emprestimoSnap.data()!;
    await validarGestorDoAlmoxarifadoTx(tx, request.auth!.uid, emprestimo.id_almoxarifado);

    if (!dados.idUsuarioRetirouCorreto)
      throw new HttpsError("invalid-argument", "Correção de retirante exige idUsuarioRetirouCorreto.");

    // Efeito efetivo corrigido: apenas o vínculo do retirante. Frasco, peso de saída,
    // instante físico original e local de uso permanecem inalterados.
    tx.update(emprestimoRef, { id_usuario_retirou: dados.idUsuarioRetirouCorreto });

    // Vínculo auditável entre operação original e operação corretora.
    tx.set(admin.firestore().collection("Historico_Frasco_Reagente").doc(), {
      id_frasco_reagente: emprestimo.id_frasco_reagente,
      id_almoxarifado: emprestimo.id_almoxarifado,
      id_gestor: request.auth!.uid,
      tipo: "CORRECAO_OPERACIONAL",
      operacao_corrigida: dados.idOperacaoCorrigida,
      tipo_correcao: dados.tipoCorrecao,
      id_usuario_retirou_anterior: emprestimo.id_usuario_retirou,
      id_usuario_retirou_novo: dados.idUsuarioRetirouCorreto,
      motivo: dados.motivo,
      timestamp: admin.firestore.FieldValue.serverTimestamp(),
    });

    const resultado = { status: "OK", operacaoCorrigida: dados.idOperacaoCorrigida };
    tx.set(operacaoRef, {
      tipo_operacao: "CORRIGIR_OPERACAO",
      operacao_corrigida: dados.idOperacaoCorrigida,
      uid: request.auth!.uid,
      payload_hash: payloadHash,
      resultado,
      status: "CONCLUIDA",
      criado_em: admin.firestore.FieldValue.serverTimestamp(),
      atualizado_em: admin.firestore.FieldValue.serverTimestamp(),
    });
    return resultado;
  });
});
\end{lstlisting}